# ChenDoc 开发者分析报告

审查日期：2026-06-19  
审查对象：`D:\desktop\bixu\js\chensdoc-claude` 当前工作树  
说明：当前工作树已有大量未提交修改；本报告只新增文档，不评价旧提交归属。

## 总评

项目模块边界总体清楚，严格 TypeScript、Zod、Fastify、Drizzle、服务端测试和构建链均已建立。主要技术债集中在新收集表模块、会话轮换、前端工程门禁、数据库双实现一致性和超大页面组件。

星级代表整改优先级，已按高到低排序。

## ★★★★★ 1. 业务状态正确性

### 发现

- `createForm()` 没有写入 `maxSubmissions`。
- `updateForm()` 使用 `Boolean(body.config.maxSubmissions)`，把任意正数写成 `true/1`。
- `allowMultiple` 已建模但提交链路未使用。
- `submissionCount` 使用读后判断再插入，并发时可能超过上限。
- 删除提交后直接 `submissionCount - 1`，异常数据下可变为负数。
- `forms.public.ts` 的 `checkbox` 出现重复 `case`；后一个分支永远不会执行。测试运行时 Vite 已给出警告。

### 更改

1. 建立 `FormSubmissionPolicy`，集中处理上限、重复、验证码、字段白名单。
2. 上限检查和计数更新放进事务，使用条件更新或数据库锁。
3. 计数以定期校准任务兜底，删除时使用 `max(count - 1, 0)`。
4. 区分单个同意框和多选题，不复用同一个 `checkbox` 类型。

## ★★★★★ 2. 会话生命周期模型

### 发现

- 前端刷新请求未等待异步 Authorization 构造：`apps/admin/src/api/request.ts:118`。
- 服务端刷新签发新 JWT，但保留旧摘要：`server/src/modules/auth/session.service.ts:94-100`。
- 新 JWT 在 `verifyAuthSessionToken()` 的摘要比较中会失败：`:115-120`。
- 注释声称保留旧 JWT 可避免并发失败，但数据模型只有一个摘要，无法同时接受新旧 token。

### 更改

1. 明确选择滑动会话或固定 JWT，不混合两套语义。
2. 滑动会话建议存 `currentDigest`、`previousDigest`、`previousValidUntil`。
3. 前端刷新使用同一 `request()` 基础设施，避免手写 fetch 漏掉网关、风险头和 await。
4. 增加 token 过期前刷新、并发刷新、旧 token 宽限、禁用用户撤销测试。

## ★★★★★ 3. 数据库实现一致性

### 发现

- MySQL `max_submissions` 是 `INT`，SQLite Drizzle schema 却使用 `{ mode: "boolean" }`：`server/src/db/schema.sqlite.ts:283`。
- SQLite 建表 SQL 又使用普通 `INTEGER`：`server/src/db/migrate.ts:192`。
- MySQL schema/DDL 基本不声明外键，SQLite 声明了大量 `.references()`；生产和测试的数据完整性行为不同。
- 收集表未进入现有 SQLite → MySQL 迁移脚本的可见映射。

### 更改

1. 建立 schema contract test，对比列名、类型、空值、默认值、唯一索引和外键策略。
2. `maxSubmissions` 在两端统一为 nullable integer。
3. 生产 MySQL 至少补 `form_submissions.form_id`、`forms.owner_id`、`shares.doc_id` 等核心外键，或写明禁用理由并做一致清理测试。
4. 迁移脚本加入 forms/form_submissions 和计数校验。

## ★★★★☆ 4. 自动化测试覆盖结构

### 现状

- 当前有 6 个服务端测试文件、49 个测试，全部通过。
- 已覆盖 gateway packet、登录风险、文档、分享、公开分享、HTML 清洗。
- 收集表、上传权限、会话刷新、TOTP、危险验证、设置管理无直接测试。
- 前端没有组件测试或端到端测试。

### 更改

1. 第一批补 `forms.service.test.ts`、`forms.public.routes.test.ts`、`session.service.test.ts`、`uploads.service.test.ts`。
2. 用 Playwright 覆盖登录 → 编辑 → 自动保存 → 分享 → 退出，以及公开表单提交。
3. 将构建警告视为 CI 失败，避免“测试通过但功能不可运行”。
4. 权限测试使用 admin、super admin、普通用户、禁用用户四类固定夹具。

## ★★★★☆ 5. 前端类型与质量门禁

### 发现

- `apps/admin` 安装了 `vue-tsc`，但 `build` 只执行 `vite build`。
- Vite 会转译 TypeScript，不会完成完整 Vue 类型检查。
- 项目没有 lint、format check、未使用导入检查、CSS 校验。
- CI 仅跑前后端 build 和服务端测试。

### 更改

1. 新增 `typecheck: vue-tsc --noEmit`。
2. `build` 前执行 typecheck，CI 独立展示。
3. 增加 ESLint + Vue + TypeScript 最小规则，不做大规模格式重写。
4. 增加 `check` 聚合脚本：typecheck、test、build、audit。

## ★★★★☆ 6. 组件规模与职责

### 发现

- `FormEditorPage.vue` 约 1645 行、55 KB；同时导入 23 KB 外部 CSS，又保留约 500 行 scoped CSS。
- `SettingsPage.vue` 约 1370 行，混合用户、日志、站点、系统维护和版本更新。
- `DocEditorPage.vue` 约 789 行，已开始承载保存、分享、版本、目录、移动抽屉等多种状态。
- `settings.service.ts` 约 830 行，承担远程资源、系统统计、用户管理、R2、站点配置。

### 更改

1. 表单拆为 `FieldPalette`、`FormCanvas`、`FieldInspector`、`FormSettings`、`FormStats`。
2. 设置按 `users/site/storage/logs/maintenance` 拆 service 和 page section。
3. 文档编辑器把 autosave、share、versions 抽成 composable。
4. 拆分以业务边界为准，避免只按行数机械切文件。

## ★★★★☆ 7. API 与 Gateway 契约

### 发现

- `gateway/client.ts` 维护大型手写 URL → action code 映射，新增接口容易漏映射。
- forms action 已写入映射，但 `shouldUseGateway()` 又排除全部 `/api/forms`，形成死配置和架构分叉。
- 表单公开提交使用 `/f/:uid/submit`，私有表单 API 使用 `/api/forms`，命名边界可接受，但缺少共享契约。

### 更改

1. 用单一 action registry 生成前端映射和后端分发表。
2. 明确 forms 私有 API 是否进入 Gateway；公开 HTML 提交保持独立即可。
3. 对每个 API 维护请求/响应 Zod schema，避免前后端类型手抄。
4. CI 增加“所有 apiPaths 均有 action 或明确豁免”测试。

## ★★★☆☆ 8. 样式架构与设计令牌

### 发现

- 主项目使用 `--cd-*`，收集表新增 `--tencent-*` 全局变量和整套第三方风格。
- `FormEditorPage.vue` 同时存在外部 CSS 和 scoped CSS，规则重复并增加覆盖顺序复杂度。
- `apps/admin/index.html:7` 引用不存在的 `/src/pages/login/login.css`；实际文件已移动到 `pages/login/css/login.css`。
- `form-list.css` 只有注释，列表实际依赖其他页面样式或全局样式。

### 更改

1. 收敛为 `--cd-*` token，表单只定义模块级语义变量。
2. 每个页面保留一个 CSS 入口；删除重复 inline style。
3. 页面自己导入 CSS，`index.html` 不引用源码页面路径。
4. 增加 CSS 构建 smoke test，检查产物中的 404 资源。

## ★★★☆☆ 9. 构建产物与性能预算

### 发现

- 路由懒加载、编辑器手动分块已做。
- `cssCodeSplit: false` 生成约 181 KB 单 CSS。
- 混淆目标 gzip 从 81.76 KB 增至 142.82 KB，增幅约 74.67%。
- 构建包含约 1.39 MB PNG。
- 构建提示登录 CSS和四个字体资源无法解析。

### 更改

1. 恢复页面级 CSS 分块，仅给登录关键样式做可靠预加载。
2. 缩小混淆范围到网关薄层；混淆不是安全边界。
3. 引入构建预算脚本，超过 JS/CSS/图片阈值即失败。
4. 清理重复背景图、Logo 和历史资产。

## ★★★☆☆ 10. 可观测性与错误语义

### 现状

- Fastify 日志有敏感字段脱敏，慢请求超过 300ms 会记录。
- 错误处理统一，操作日志和安全日志已有独立模型。
- 慢请求只记录 action code、状态和耗时，缺少数据库阶段、外部 R2 阶段。
- 多处前端 catch 静默忽略，例如会话刷新失败。

### 更改

1. 给请求贯穿 requestId，并写入审计日志。
2. 为 DB、R2、Gateway 解包记录结构化耗时，不记录正文。
3. 前端静默错误至少记录分类计数，用户态只显示可行动提示。
4. 建立健康检查：DB 查询、R2 可选探测、构建版本、迁移版本。

## ★★☆☆☆ 11. 部署、备份与恢复工程

### 现状

- 部署前会检查密钥、MySQL、依赖审计、备份、迁移和构建。
- MySQL 备份使用 `--single-transaction`，基础正确。
- 没有备份保留策略、压缩/加密、异地复制和恢复演练脚本。
- `start.sh` 会在生产机全局安装 PM2，降低部署可复现性。

### 更改

1. 增加备份保留天数、校验和、可选加密和异地路径。
2. 增加只读恢复 smoke test，不直接覆盖生产库。
3. PM2 固定版本或使用 systemd；部署过程不要隐式全局安装。
4. 在系统状态展示最近成功备份，不展示连接串。

## ★★☆☆☆ 12. 文档与实现漂移

### 发现

- `PRODUCT.md`、`DESIGN_LANGUAGE.md` 只指向 `超记忆副本.md`，但该文件仍写明此 checkout 是“历史/对照 checkout”。当前任务却以它为工作区。
- `.env.example` 的 idle timeout 注释写 90 分钟，`env.ts` 默认值为 4 小时。
- README 说“个人站点、小团队”，核心规范又强调站长本人/极小私有流程。
- 收集表已进入代码，但项目地图和安全测试优先级未完整纳入。

### 更改

1. 确认此 checkout 的真实角色，更新根路径描述。
2. 环境变量默认值从代码生成文档，避免双写。
3. 新模块合并时同步 FILE_GUIDE、威胁模型和测试矩阵。

## 建议的三批改动

### 第一批：阻断缺陷

- 修复会话刷新。
- 修复表单 CSP、上限、重复提交、验证码、checkbox 分支。
- 补对应服务端和浏览器测试。

### 第二批：工程门禁

- 加入 vue-tsc、契约测试、schema parity test、构建预算。
- 修复登录 CSS、字体和产物 404。

### 第三批：结构治理

- 拆 FormEditor、SettingsPage、settings.service。
- 收敛 token、Gateway registry、备份恢复流程。

## 本次验证

- `npm.cmd --workspace @chendoc/server run test`：6 个文件、49 个测试全部通过；出现重复 `case "checkbox"` 构建警告。
- `npm.cmd --prefix server run build`：通过。
- `npm.cmd --prefix apps/admin run build`：通过；出现登录 CSS和四个字体资源无法解析警告。
- `npm.cmd audit --omit=dev --registry=https://registry.npmjs.org --json`：生产依赖漏洞 0。
- `npm.cmd audit signatures --registry=https://registry.npmjs.org`：582 个包签名已验证，114 个包有 attestations。

