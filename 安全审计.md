# ChenDoc 多维安全审计

审查日期：2026-06-19  
审查对象：`D:\desktop\bixu\js\chensdoc-claude` 当前工作树  
方法：源码审查、服务端测试、前后端构建、npm 生产依赖审计、签名审计。未进行公网渗透、真实 MySQL/R2 攻击测试。

## 风险标尺

- `★★★★★`：立即修复。可导致关键链路失效、权限/内容风险或安全控制形同虚设。
- `★★★★☆`：高风险。应在下次发布前修复。
- `★★★☆☆`：中风险。需要排期和回归测试。
- `★★☆☆☆`：低风险或纵深防御。
- `★☆☆☆☆`：已具备较好控制，持续保持。

## 总结

未发现依赖已知漏洞，网关加密、密码哈希、文档静态加密、HTML 清洗、危险操作验证和日志脱敏均有实际实现。最需要处理的是公开表单 CSP、会话续期、表单防滥用、动态 HTML 输出、密钥预检遗漏和生产数据库完整性差异。

## ★★★★★ 1. 浏览器内容安全策略

### 发现

公开表单脚本使用随机 nonce：`server/src/modules/forms/forms.public.ts:253`、`:310`。但 `forms.public.routes.ts` 只设置 Content-Type，没有设置包含该 nonce 的 CSP。全局 Helmet 策略是 `script-src 'self'`：`server/src/app.ts:74-82`。

### 影响

浏览器会阻止表单页的内联脚本。提交、城市联动、成功页等功能不可用。用户可能误以为表单已发布可收集数据，属于高可用性和数据完整性风险。

### 修复

参照 `server/src/modules/public/public.routes.ts:76-85`，在 `/f/:formUid` 响应中设置 `script-src 'self' 'nonce-...'`。nonce 应由路由生成并传入渲染器。增加浏览器级 CSP 测试。

## ★★★★★ 2. 身份会话与令牌轮换

### 发现

1. `apps/admin/src/api/request.ts:118` 未等待 `buildAuthorization()`。
2. `renewAuthSession()` 签发新 JWT 后不更新 `keyEncrypted`。
3. 验证又要求数据库摘要等于当前 token 摘要：`server/src/modules/auth/session.service.ts:115-120`。

### 影响

自动续期请求本身可能无有效认证；即使续期成功，新 token 也会被服务端拒绝。长时间编辑会话不稳定。反复跳转登录会诱发未保存数据、重复提交和用户绕过安全提示的错误操作。

### 修复

前端等待 Authorization；服务端采用带宽限的双摘要轮换或原子替换；限制同一会话并发刷新；补 token 重放和轮换测试。

## ★★★★☆ 3. 输出编码与 DOM 注入

### 发现

- 公开表单成功页把用户值直接拼入 `innerHTML`：`server/src/modules/forms/forms.public.ts:417`。
- 文档列表将预览文本解码为 `<`/`>` 后交给 `v-html`：`apps/admin/src/pages/docs/DocListPage.vue:225-234`、`:267-272`、`:469`、`:566`。
- 全局 CSP 禁止 inline script，能降低常见事件处理器攻击，但不能替代输出编码。

### 影响

表单处当前主要是提交者自身上下文，但仍会破坏 DOM；文档预览若跨用户/管理员可见，风险升级为存储型注入。未来 CSP 放宽会放大问题。

### 修复

用 `textContent` 创建值节点；高亮预览使用 Vue vnode 或先严格转义、只允许 `<mark>`；增加恶意实体、SVG、事件属性、畸形标签测试。

## ★★★★☆ 4. 表单滥用与容量控制

### 发现

- `maxSubmissions` 创建时未保存，更新时被布尔化。
- SQLite schema 还把该字段定义为 boolean：`server/src/db/schema.sqlite.ts:283`。
- `allowMultiple` 不执行。
- 第三次提交要求验证码，但公开页不发送验证码字段。
- IP 限流使用进程内 Map；多实例不共享，重启即清空。
- 上限检查和插入不是原子操作。

### 影响

攻击者可绕过重复/总量限制；合法用户也可能被不可完成的验证码流程阻断。大量提交会占用数据库、日志和管理界面。

### 修复

修正字段类型；使用数据库原子计数；多实例部署用 Redis/数据库限流；验证码端到端接通；限制字段数量、单值长度、提交 JSON 总字节数和 User-Agent 长度。

## ★★★★☆ 5. 密钥治理与加密恢复

### 现状

- JWT、配置、RSA 私钥、文档内容均要求独立密钥。
- AES-256-GCM 使用随机 12 字节 IV，方向正确。
- 生产启动会拒绝常见默认密钥。

### 发现

- `scripts/preflight-deploy.js` 检查 JWT、配置、RSA 密钥，却漏检 `CHENDOC_DOCUMENT_ENCRYPTION_KEY`。
- `documentCrypto.ts` 记录 `keyVersion`，解密时却始终只使用当前密钥，未按版本选钥。
- 解密失败返回明文 fallback 或空内容，没有高优先级告警；错误密钥可能表现为文档空白。

### 修复

预检加入文档密钥；实现版本化 keyring；部署前检测数据库中使用的 key version；密钥轮换先重加密再切换；解密失败记录安全事件且禁止静默覆盖保存。

## ★★★★☆ 6. 高危操作授权边界

### 现状

- 删除用户、禁用用户、修改站点、R2 保存、永久删除文档等多数操作有二次危险验证。
- 超级管理员保护已独立实现。

### 发现

- `DELETE /api/uploads/:id` 只要求 admin，不要求危险验证；service 按 ID 删除任意 R2 对象和记录，没有 actor/所有权参数：`server/src/modules/uploads/uploads.routes.ts:14`、`uploads.service.ts:338` 附近。
- 查看和重置普通用户密码接口只要求 admin，未使用危险验证；虽不能查看明文，但重置会撤销会话。
- 启用用户不要求危险验证，禁用却要求，策略不对称。

### 修复

上传删除至少校验 actor、文档归属和引用；批量/跨用户删除要求危险验证。用户密码重置、启用管理员账号也纳入危险验证和审计。

## ★★★☆☆ 7. 凭据强度与二次认证

### 现状

- 优先 Argon2id，缺失时回退 bcrypt cost 12。
- TOTP 支持 ±1 时间窗、恢复码单次使用、危险操作二次验证。

### 发现

- 用户和管理员密码最低仅 6 位：`server/src/utils/password.ts:4`、`:48`；与 `.env.example` 的“12+ 字符并含复杂度”描述不一致。
- TOTP 恢复码以可逆密文保存，并可通过 GET 再次列出剩余明文码。
- TOTP pending secret 存在进程内 Map，重启后设置流程失效；多实例不共享。

### 修复

最低 12 位，优先长度和泄漏密码检查；恢复码只展示一次，数据库存独立哈希；TOTP pending secret 存短期数据库记录或签名临时票据。

## ★★★☆☆ 8. 隐私与数据最小化

### 发现

- 表单提交永久保存 IP 和完整 User-Agent。
- 管理界面展示 IP 统计和提交明细。
- 没有可配置保留期、匿名化、隐私说明或一键删除某表单全部个人数据。

### 影响

对个人站点而言，这些数据可能超出实际需要；备份和导出会扩大暴露面。

### 修复

表单发布时可配置隐私说明和保留天数；默认截断/哈希 IP，精确 IP仅在防滥用窗口内保留；User-Agent 限长并可关闭保存；导出记录审计事件。

## ★★★☆☆ 9. 上传与对象存储

### 现状

- 扩展名、MIME、大小三重校验。
- 完成上传时用 HeadObject 校验大小和 Content-Type。
- object key 随机化，上传票据 10 分钟过期并绑定 user、object、size、kind。

### 发现

- 允许公开 URL，上传内容一旦完成可能长期可访问，删除文档只把 upload 的 `docId` 置空，不删除对象。
- ZIP、Office、PDF 等文件完全依赖声明 MIME，没有恶意文件扫描。
- R2 CORS 使用 `AllowedHeaders: ["*"]`，虽来源受配置限制，仍可进一步收紧。

### 修复

明确孤儿附件生命周期；增加未完成/未引用对象清理；可选接入病毒扫描或隔离下载；CORS 只允许实际需要的请求头。

## ★★★☆☆ 10. 数据库完整性与租户隔离

### 发现

- SQLite schema 有外键引用，MySQL Drizzle schema和 DDL 基本只建索引，不建外键。
- 生产 MySQL 和测试 SQLite 对删除、孤儿记录、类型转换行为不同。
- `tenantKey` 固定默认值，当前不是完整多租户边界，不应被误认为租户隔离。

### 修复

为核心关系增加 MySQL 外键或显式一致性任务；CI 使用真实 MySQL 容器跑权限和删除测试；文档明确系统是单站点多账号，不宣称多租户。

## ★★★☆☆ 11. 网关与传输边界

### 现状

- Gateway 使用 RSA-OAEP、AES-256-GCM、HMAC、challenge、timestamp、nonce、防重放和加密响应。
- 认证 JWT 又做客户端包装，并通过 Authorization 发送。

### 发现

- 全部 forms API 被 `shouldUseGateway()` 排除，但 action 映射仍存在，架构意图不一致。
- 加密网关不能替代 HTTPS；`connect-src` 允许任意 HTTPS，前端被注入后仍可能向外发送数据。
- 浏览器 token 位于 sessionStorage，任何同源 XSS 都可读取。

### 修复

明确私有 forms API是否纳入 Gateway；生产强制 HTTPS/HSTS 由反代配置；按实际远程域名收紧 `connect-src`；把 XSS 防护视为 token 保护核心。

## ★★☆☆☆ 12. 日志、备份与事件响应

### 现状

- Fastify logger 和异步日志队列均做敏感字段脱敏。
- 部署前执行 MySQL 一致性备份。
- 操作日志覆盖高危设置和用户操作。

### 发现

- 备份未自动压缩、加密、轮换、异地复制，也没有恢复演练。
- 日志保留期和磁盘上限不清晰。
- 表单删除、提交删除、上传删除的审计覆盖不一致。

### 修复

定义备份 RPO/RTO、保留期、加密和恢复测试；日志按大小/天数轮换；所有数据导出、跨用户删除、附件删除写安全审计日志并带 requestId。

## ★☆☆☆☆ 13. 依赖与供应链

### 结果

- 官方 npm registry 生产依赖审计：0 个已知漏洞。
- 582 个包具有已验证 registry signatures。
- 114 个包具有 attestations。
- CI 使用 `npm ci`、Node 20、build、test、audit。

### 保持

固定 lockfile；Dependabot/Renovate 分批升级；高风险依赖升级先跑公开分享、网关、上传、数据库 smoke test。当前默认 npmmirror 不支持 audit endpoint，安全命令应固定官方 registry。

## 修复优先顺序

1. 表单 CSP、会话刷新、表单上限/重复/验证码。
2. DOM 输出编码、上传删除授权、密码重置危险验证。
3. 文档密钥预检/keyring、密码策略、恢复码哈希。
4. MySQL 一致性、隐私保留、备份恢复演练。

## 验证记录

- 服务端测试：49/49 通过。
- 服务端 TypeScript 构建：通过。
- 管理端 Vite 构建：通过，但有登录 CSS、字体资源警告。
- npm 生产依赖审计：0 漏洞。
- npm 签名审计：通过。
- 未执行：真实 MySQL、真实 R2、公网页面渗透、浏览器自动化安全测试。

