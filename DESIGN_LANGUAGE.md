# ChenDoc Design Language

ChenDoc v2.2.0 定位为商业化文档系统。界面服务于登录、文档管理、编辑、公开阅读和系统配置，不承担产品宣传页职责。

## 产品气质

- 产品感：页面首先呈现品牌、文档和操作入口，避免大段介绍和卖点堆叠。
- 专业感：主色使用可信蓝，成功状态使用绿色，危险操作使用红色。
- 品牌感：二次元角色只作为 Logo、登录背景和少量品牌资产，不成为页面主体。
- 编辑感：编辑器以标题、正文、目录、保存状态和分享动作为核心，减少后台表格感。
- 轻量化：继续使用 Vue、Tiptap、lucide 和本地字体，不引入重型 UI 框架、动画库或远程字体依赖。

## Design System

- 字体：产品界面统一使用本地 AlibabaPuHuiTi 和系统字体；长文阅读可使用现有 SourceHanSerif。
- 颜色：`#2563EB` 为主操作色，`#059669` 为成功状态，`#0F172A` 为标题墨色，`#F6F8FB` 为平台背景。
- 间距：使用 4/8px 节奏，页面留白服务于扫描效率。
- 圆角：主要控件、输入框和卡片保持 8px。
- 边框：使用 `#DBE4F0` 形成清晰层级，避免厚重装饰。
- 阴影：只用于浮层、登录框和阅读纸面，不用大面积玻璃效果。

## 页面规则

- 登录页：只保留 Logo、陈书、文档管理平台、账号、密码、验证码、登录、注册。
- 文档页：使用文档列表、状态和分享路径组织信息，不做数据库式表格。
- 编辑器：正文画布优先，工具栏只保留功能控件和原生提示。
- 分享页：阅读优先，标题和正文自然排布，不像配置页面。
- 系统设置：入口短标签化，避免每张卡片解释功能。

## 二次元规范

- 可以作为品牌资产、记忆点和登录背景层。
- 不做动漫海报，不抢占登录和编辑任务。
- 不使用大量插画、贴纸、角色对白或剧情式表达。

## ui-ux-pro-max 应用

- 使用 `--design-system` 生成商业文档平台方向，并筛掉 AI 助手式推荐。
- 采用文档管理类色彩结果：专业蓝、浅灰背景、白色纸面、绿色成功状态。
- 采用简体中文可读字体策略，映射到本地字体，避免远程字体加载。
- 使用 UX 检查项：可见焦点、输入框可识别、提交反馈、移动端触控间距、减少横向溢出。
