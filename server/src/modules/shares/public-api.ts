/**
 * @fileoverview Shares 模块公共 API 导出
 *
 * 模块职责：文档分享创建、更新、删除、审核、公开访问验证
 *
 * 功能划分：
 * - 分享 CRUD（需认证）
 * - 分享审核（管理员）
 * - 公开分享访问（无需认证）
 * - 密码保护与验证
 * - JWT 令牌签名/验证
 *
 * 依赖说明：
 * - docs 模块：访问控制检查（canAccessDocument）
 *
 * @module shares
 */

// ============================================================================
// 类型导出
// ============================================================================

export type { PublicShareResolution } from "./shares.service.js";

// ============================================================================
// 错误类
// ============================================================================

export { ShareAccessError } from "./shares.service.js";

// ============================================================================
// 分享管理服务（需认证）
// ============================================================================

export {
  createOrGetShare,
  createOrGetShareByDocUid,
  updateShare,
  getShareByDoc,
  getShareByDocUid,
  deleteShare,
  adminSharePayload,
} from "./shares.service.js";

// ============================================================================
// 分享审核服务（管理员）
// ============================================================================

export {
  listUserShareReviews,
  reviewUserShare,
} from "./shares.service.js";

// ============================================================================
// 公开分享访问服务（无需认证）
// ============================================================================

export {
  resolvePublicShare,
  getPublicShare,
  publicDocPayload,
  recordPublicShareView,
  verifySharePassword,
} from "./shares.service.js";

// ============================================================================
// JWT 令牌服务
// ============================================================================

export {
  signShareAccessToken,
  verifyShareAccessToken,
} from "./shares.service.js";

// ============================================================================
// 缓存失效（供 docs 模块调用）
// ============================================================================

export { invalidateDecryptedDocCache } from "./shares.service.js";
