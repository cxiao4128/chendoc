# ChenDoc 更改必读规范

本文档用于约束之后每次修改 ChenDoc 代码、部署包前必须遵守的规则。纯 README 开源页文案更新不算版本发布；除此之外，只要会提交代码或交付给部署使用，都按一次版本变更处理。

## 版本号必须同步更新

除纯 README 文案更新外，每次更改都必须递增一个版本号，不能只改功能不改版本。当前展示版本使用 `v1.03` 这种格式，源码包版本使用 `1.0.3` 这种 semver 格式，两者必须对应。

每次发版前至少同步更新这些位置：

- `README.md` 的当前版本。
- 根目录 `package.json` 的 `version`。
- `apps/admin/package.json` 的 `version`。
- `server/package.json` 的 `version`。
- `package-lock.json` 中 ChenDoc 根包、`apps/admin`、`server` 的版本。
- `apps/admin/src/pages/settings/SettingsPage.vue` 中系统管理页展示的 `APP_VERSION`。

版本规则：

- `v1.03` 对应 `1.0.3`。
- `v1.04` 对应 `1.0.4`。
- 之后每次提交功能、修复、文档或部署包，都继续递增。

## 每次更改后的必要动作

改完源码后必须做以下检查：

- 确认 `git status` 中只包含本次相关文件。
- 不能手改 `dist`、`server/dist`、`server/public/admin` 等构建产物来代替源码修改。
- 前端改动后运行 `npm.cmd --prefix apps/admin run build`。
- 后端改动后运行 `npm.cmd --prefix server run build`。
- 涉及后端逻辑时运行 `npm.cmd --prefix server run test`。
- 打部署包前重新构建，确保包内源码和构建结果来自同一版本。

## 不允许随意改动的核心逻辑

以下逻辑未经明确要求不得改动：

- 不改 R2 上传核心链路、签名上传流程、对象存储写入逻辑。
- 不改公开分享访问判断和分享审核边界。
- 不为了“看起来更规范”重构全项目目录。
- 不做大规模 UI 重写，除非明确要求。
- 不删除后台管理页已有核心入口：注册卡密、R2 设置、文章删除、回收站。
- 不把邀请码入口重新放回左侧导航；注册卡密从后台管理卡片进入。
- 不把操作日志恢复成英文动作名；页面展示必须使用中文动作描述。
- 不把“超级管理员权限”改回“管理员权限”。
- 不绕过用户登录禁用逻辑、用户注销逻辑和权限提升逻辑。

## 后台管理页约束

后台管理页当前结构应保持：

- 上方使用卡片入口。
- 操作日志作为卡片入口，点击后查阅。
- 站点外观作为卡片入口，点击后配置。
- 用户管理作为卡片入口，点击后查阅用户信息、IP、文档数量，并执行权限提升、禁止登录、注销用户等操作。
- 页面底部展示当前版本、检查更新按钮和开源链接。

新增功能时应优先接入现有卡片和面板结构，避免重新铺一套割裂的后台界面。

## 打包和部署约束

部署包不得包含以下内容：

- `.git`
- `node_modules`
- `apps/admin/node_modules`
- `server/node_modules`
- `data`
- `.env`
- `.env.local`
- `*.log`
- `*.sqlite`
- 历史 `*.zip`
- `release`

压缩包内部不得再套一层 `chendoc` 文件夹。服务器上解压后，应能在解压目录直接看到 `deploy.sh`、`package.json`、`apps`、`server` 等项目文件。

部署运行命令固定为：

```bash
bash ./deploy.sh
```

首次部署或需要重新初始化管理员时，按 `.env` 中的配置确认后再执行：

```bash
CHENDOC_INIT_ADMIN=1 bash ./deploy.sh
```
