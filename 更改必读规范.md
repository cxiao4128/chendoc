# ChenDoc 更改必读规范

更新时间：2026-06-30
当前版本：`3.3.0`

本文件只放修改、版本、验证、打包约束。项目记忆看 `超记忆副本.md`。

## 必守规则

- 不手改 `apps/admin/dist`、`server/dist`、`server/public/admin`。
- 不提交 `.env`、数据库、日志、备份包、运行数据、密钥。
- 不绕过生产 `POST /api/gateway` 加密网关。
- Gateway 请求和响应都保持 packet-wrapped。
- 前端业务请求继续使用内存中的加密 JWT `Authorization`；只允许服务端设置 `HttpOnly + SameSite=Strict + Secure(生产)` 会话恢复 Cookie，禁止把令牌写入 JavaScript 可读的 localStorage/sessionStorage。
- 不改 R2 预签名上传链路，除非明确任务和测试。
- 不改公开分享访问边界，除非明确任务和测试。
- 不绕过用户禁用、注销、权限提升、超级管理员边界。
- 不把“超级管理员权限”改回“管理员权限”。
- 不把操作日志动作恢复成英文展示。
- 不做大规模 UI 重写。
- 不引入新 UI 框架处理小问题。

## 版本规则

ChenDoc 使用完整 semver：`x.y.z`。

- `x`：核心架构、数据模型、部署模型、认证体系的大版本变化。
- `y`：新增功能、系统增强、安全增强、构建体系优化、后台增强。
- `z`：Bug 修复、文案修复、样式修复、小型性能优化、发布文档修复。

必须同步版本的位置：

- `package.json`
- `apps/admin/package.json`
- `server/package.json`
- `package-lock.json`
- `apps/admin/src/pages/settings/SettingsPage.vue` 的 `APP_VERSION`
- `README.md`
- `CHANGELOG.md`

必须递增版本：

- 改源码并准备提交或交付部署。
- 改数据库结构、迁移、部署脚本、构建脚本。
- 改 README/CHANGELOG 中面向发布的版本内容。
- 打发布包或自用部署包。

不用自动递增版本：

- 只改内部说明、记忆、Agent/Claude/Codex 上下文文件。

## 验证规则

- 前端源码改动：`npm.cmd --prefix apps/admin run build`
- 后端源码改动：`npm.cmd --prefix server run build`
- 后端行为、认证、网关、分享、上传、数据库、安全改动：`npm.cmd --workspace @chendoc/server run test`
- 跨 workspace 或发包：`npm.cmd run build`
- 每次结束前跑 `git diff --check`

## 后台入口保护

不要删除这些入口：

- 文档
- 回收站
- 审核
- 安全中心
- 模板中心
- 知识库
- 系统管理
- 注册卡密/邀请码
- R2 对象存储
- 文档危险删除
- 用户与权限
- 日志审计
- 版本更新

新增功能优先接入现有导航、卡片、面板。

## 打包规则

部署包不得包含：

- `.git`
- `node_modules`
- `apps/admin/node_modules`
- `server/node_modules`
- `data`
- `.env`
- `.env.local`
- `.tmp`
- `backups`
- `logs`
- `*.log`
- `*.sqlite`
- 历史 `*.zip`
- `release`

压缩包内部不得再套一层 `chendoc`。解压目录应直接看到 `deploy.sh`、`package.json`、`apps`、`server`。

双包规则：

- Cloudflare Pages 包：只含 `apps/admin/dist` 的可部署静态文件、运行时来源配置、`_headers` 和 `_redirects`；不得包含源码、服务端、环境文件或 source map。
- Cloudflare Git 构建统一执行根命令 `npm run build:cloudflare-pages`，输出目录固定为 `apps/admin/dist`；API 与展示域名从 Pages 环境变量注入，缺失时构建必须失败。
- 正常服务器包：只含公开源码、配置模板和部署脚本；不含内部记忆、密钥、数据库、日志、node_modules、历史包和生成目录。
- 两包统一由 `scripts/release.js` 生成并输出 SHA-256；PowerShell 只转发参数，不维护第二套过滤规则。

## 不要手改

- `node_modules/`
- `apps/admin/dist/`
- `server/dist/`
- `server/public/admin/`
- `data/`
- `.tmp/`
- `backups/`
- `logs/`
- `release/*.zip`
- `*.log`
- `*.sqlite`
- `.env`

## CSS 拆分规范

多模块页面按功能拆分 CSS，每个文件只负责一个模块：

| 文件 | 负责 |
|------|------|
| `page.css` | 主入口，只有 `@import` |
| `page-header.css` | 头部、标题、操作按钮 |
| `page-stats.css` | 统计卡片 |
| `page-layout.css` | 双栏布局、侧边栏 |
| `page-table.css` | 表格、工具栏、筛选、空态、骨架屏 |
| `page-responsive.css` | 所有媒体查询 |

主入口只写 `@import "./page-*.css"`，按模块顺序导入，响应式 CSS **最后**导入确保覆盖。

修改规则：
- 改哪个模块就改哪个文件
- 响应式断点只写在 `page-responsive.css`
- 不要在一个模块文件里写另一个模块的样式

## Vue 组件逻辑规范

- 筛选状态要用筛选后的数据判断显示（如 `filteredDocs.length` 而非 `docs.length`）
- 批量操作后要清除已选状态：`selectedDocUids.value = []`
- API 返回值字段名要和服务端一致（用 `Grep` 确认 `response.restoredDocUids` vs `response.restored`）
