# ChenDoc 重度用户分析报告

审查日期：2026-06-19  
审查对象：`D:\desktop\bixu\js\chensdoc-claude` 当前工作树  
定位：个人文档管理、知识发布、公开分享、轻量数据收集。

## 结论

ChenDoc 的文档主链路已具备长期使用价值：文档列表、自动保存、历史版本、分享审核、回收站、移动端分流都不是空壳。当前最大问题不是“功能少”，而是新加入的收集表和会话续期存在断链，部分页面风格、反馈和真实能力不一致。

建议先修可靠性，再做视觉扩张。优先级按 `★★★★★` 到 `★☆☆☆☆` 排序。

## ★★★★★ 1. 日常连续使用可靠性

### 现状

- 编辑器有自动保存、保存失败重试、离开前保存确认，基础体验可靠。证据：`apps/admin/src/pages/docs/DocEditorPage.vue:248`、`:435`、`:453`。
- 登录会话自动续期实际不可用：`apps/admin/src/api/request.ts:118` 把异步 `buildAuthorization()` 直接放入请求头，结果会变成错误值；服务端续期后又未更新 token 摘要，见 `server/src/modules/auth/session.service.ts:94-100`。
- 重度用户连续写作接近两小时后，容易突然掉登录态。未保存内容虽有保护，但分享、上传、保存会被中断。

### 建议更改

1. `await buildAuthorization()` 后再调用 `/api/auth/refresh`。
2. 会话轮换改为“当前 token 摘要 + 短暂旧 token 宽限”，或更新摘要并串行刷新。
3. 增加 90 分钟持续编辑、刷新后继续保存、并发请求三类回归测试。
4. 页面顶部显示“会话即将过期/已恢复”，不要只在 API 失败后跳登录。

## ★★★★★ 2. 收集表真实可用性

### 现状

- 收集表入口、编辑器、公开页、提交列表、导出界面已经形成完整外观。
- 公开表单生成了带 nonce 的脚本，但 `/f/:formUid` 没有把 nonce 写入 CSP；全局 CSP 仅允许 `script-src 'self'`。浏览器会阻止内联提交脚本。证据：`server/src/modules/forms/forms.public.ts:253`、`:310`，`server/src/modules/forms/forms.public.routes.ts:7`，`server/src/app.ts:74-82`。
- “最多提交数”创建时未保存，更新时被转成布尔值。证据：`server/src/modules/forms/forms.service.ts:264-272`、`:317`。
- “允许重复提交”只存储，提交时没有执行限制。
- 第三次提交要求验证码，但公开页未提交 `captchaId/captchaCode`，用户只能收到错误，无法完成验证。证据：`forms.service.ts:413-421`、`forms.public.ts:367-372`。

### 建议更改

1. 给表单页设置与 nonce 匹配的 CSP，做真实浏览器提交测试。
2. 修正 `maxSubmissions` 写入类型；创建、编辑、读取、并发上限全部测试。
3. 定义“不允许重复”的身份判定：个人系统建议使用匿名提交 cookie + IP 辅助，不要只靠 IP。
4. 接入现有验证码组件，错误后保持已填内容。
5. 在功能修复前，把收集表标为“实验功能”，避免用户误发正式报名链接。

## ★★★★☆ 3. 文档工作台的信息密度

### 现状

- 文档筛选、搜索、批量操作、存储概览、最近动态集中在同一页，能力完整。
- 重度使用时右侧“存储概览、快捷操作、最近动态”和顶部筛选同时抢注意力；核心任务仍是找文档、打开、继续写。
- “上传文档”和“导入文档”当前调用同一个文件选择动作，用户无法理解差异。证据：`apps/admin/src/pages/docs/DocListPage.vue:594-598`。
- 所有者列在个人系统中价值低，长期占用横向空间。

### 建议更改

1. 默认保留文档、状态、更新时间、分享、操作；所有者列只在多用户模式显示。
2. 合并“上传文档/导入文档”，或明确区分“附件文档”和“解析正文”。
3. 右侧栏允许折叠并记住状态。
4. 把“最近动态”改为可点击的真实事件，不重复文档列表。

## ★★★★☆ 4. 编辑与版本恢复信心

### 现状

- 保存状态、失败重试、离开拦截、版本恢复前强制保存都做得好。
- 历史版本只显示最近 50 条，版本快照至少间隔 10 分钟，属于合理控制。证据：`server/src/modules/docs/docs.service.ts:20-21`、`:614-626`。
- 用户仍看不到版本差异，只能按时间猜测。
- 上传失败没有明确的断点续传或重新上传入口。

### 建议更改

1. 历史版本增加标题、字数、修改者、差异摘要。
2. 恢复前提供只读预览和“恢复为副本”。
3. 上传块显示失败原因、重试、替换、删除。
4. 大文档增加字数、同步耗时和最后一次服务端确认时间。

## ★★★★☆ 5. 分享与发布边界

### 现状

- 分享密码、短码、审核、过期时间、分享开关已经覆盖个人发布常用需求。
- “已发布”“已分享”“审核中”同时存在，普通用户容易把文档状态和公开可访问状态混为一谈。
- 分享打开按钮在审核中、关闭、过期时需要更明确解释，而不是只禁用。

### 建议更改

1. 统一为一条状态链：草稿 → 已发布 → 待审核 → 已公开/已拒绝/已关闭。
2. 分享面板直接显示“谁能访问、是否需要密码、何时失效”。
3. 增加复制成功后的链接、有效期和权限摘要。
4. 分享审核拒绝后提供“修改并重新提交”单一主按钮。

## ★★★☆☆ 6. 找回与知识发现

### 现状

- 搜索支持标题和正文预览，高亮关键词。
- 模板中心可以创建真实文档；知识库聚合发布/分享内容。
- 模板和知识库目前更像过滤入口，尚未形成重度用户需要的组织能力。

### 建议更改

1. 搜索增加空间、标签、状态、更新时间组合筛选。
2. 保存最近搜索和常用筛选。
3. 模板增加“从当前文档保存为模板”。
4. 知识库增加目录、反向链接或相关文档；不要扩成企业协作。

## ★★★☆☆ 7. 移动端短操作

### 现状

- 路由和布局明确区分移动端，文档列表、编辑器、回收站都有移动结构。
- 收集表编辑器采用三栏桌面模型，`900px` 以下直接纵向堆叠，手机上会很长，属性编辑成本高。
- 提交记录表在手机上主要依赖横向滚动。

### 建议更改

1. 手机端收集表改为“字段列表 → 字段设置抽屉 → 预览”三步，而非三栏堆叠。
2. 提交记录手机端改卡片，IP 和 User Agent 放详情。
3. 移动编辑器保留正文、保存、分享三个高频操作，其余放底部抽屉。

## ★★★☆☆ 8. 视觉一致性与可读性

### 现状

- 登录页和文档区具有清晰的陈书品牌识别。
- 收集表单独引入“腾讯文档风格”绿色/蓝色变量，与陈书淡紫、克制蓝体系冲突。证据：`apps/admin/src/pages/forms/css/form-editor.css`。
- 登录页 CSS 仍通过 `apps/admin/index.html:7` 引用已不存在的 `/src/pages/login/login.css`，构建会保留无效 URL。
- 字体目录只有 `README.md`，构建提示四个 WOFF2 资源无法解析，最终依赖系统字体。

### 建议更改

1. 收集表改用 `--cd-*` 全局 token，保留自己的布局，不复制第三方品牌语言。
2. 登录样式由 `LoginPage.vue` 显式导入 `./css/login.css`，删除 HTML 中源码路径。
3. 补齐字体文件或删除无效字体声明，避免闪烁和跨平台差异。
4. 低对比淡紫文字提高到 WCAG AA；图标按钮保持可见焦点。

## ★★☆☆☆ 9. 加载性能与首屏稳定

### 现状

- 路由按页面懒加载，编辑器依赖分块，方向正确。
- 当前构建中主 CSS 约 181 KB；前端混淆使目标 JS 从 gzip 81.76 KB 增至 142.82 KB。
- 存在约 1.39 MB PNG 资产进入构建。
- `cssCodeSplit: false` 让登录页也携带后台、表单等页面样式。

### 建议更改

1. 登录/注册关键 CSS 单独内联或独立小文件，后台 CSS 延迟加载。
2. 只混淆真正敏感的网关客户端薄层，不混淆普通业务页面。
3. PNG 转 WebP/AVIF，设置尺寸和缓存预算。
4. 建立首屏预算：登录首屏 gzip JS < 120 KB、关键 CSS < 40 KB、图片按视口选择。

## ★★☆☆☆ 10. 管理反馈与自助恢复

### 现状

- 安全中心、系统状态、操作日志、R2 测试、回收站形成了不错的自维护闭环。
- 部分维护动作只返回一句成功消息，缺少执行前影响范围和执行后明细。
- 表单收集 IP、User Agent，但界面没有保留期限或隐私提示。

### 建议更改

1. 清理、导出、永久删除前显示对象数量和不可逆范围。
2. 系统管理增加最近备份路径、大小、校验结果；不要展示数据库密码。
3. 表单发布页提供可配置隐私说明和数据保留天数。

## 推荐落地顺序

1. 修复公开表单 CSP、提交上限、重复提交和验证码。
2. 修复会话自动续期。
3. 修复登录 CSS 引用、字体缺失、收集表视觉漂移。
4. 完善分享状态文案和版本差异预览。
5. 再做工作台密度、移动收集表和性能预算。

## 已确认的优点

- 文档自动保存和离开保护完整。
- 文档版本有合并和数量上限。
- 分享公开内容经过服务端清洗。
- 回收站、审核、安全中心不是演示页面。
- 桌面和移动路由边界清楚。

